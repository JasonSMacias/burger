// got rid or orm for now and put it in config, negating the need for this model.  May come back to re-structure app and include orm/models if time permits.

// let orm = require("../config/orm.js");

// let burger = {
//   selectAll: orm.selectAll

// };


// module.exports = burger;